import React from 'react';

const UserRepo = () => {
    return(
        <div>
            Your Repository is NOW empty !<br/>
            <button>create your repository!</button>
        </div>
    );
};

export default UserRepo